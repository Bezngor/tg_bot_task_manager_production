# Инструкция по развертыванию Telegram-бота

Данная инструкция описывает процесс автоматического развертывания Telegram-бота на удаленном сервере через GitHub Actions.

## Содержание

1. [Подготовка GitHub репозитория](#подготовка-github-репозитория)
2. [Подготовка удаленного сервера](#подготовка-удаленного-сервера)
3. [Настройка секретов в GitHub](#настройка-секретов-в-github)
4. [Настройка .env файла на сервере](#настройка-env-файла-на-сервере)
5. [Запуск деплоя](#запуск-деплоя)
6. [Проверка работы](#проверка-работы)
7. [Устранение проблем](#устранение-проблем)

---

## Подготовка GitHub репозитория

1. Убедитесь, что все файлы проекта закоммичены и запушены в репозиторий
2. Проверьте наличие файла `.github/workflows/deploy.yml`
3. Убедитесь, что ветка `main` (или `master`) содержит актуальный код

---

## Подготовка удаленного сервера

### Требования к серверу

- Ubuntu 20.04+ или Debian 11+ (рекомендуется)
- Минимум 1 GB RAM
- Минимум 10 GB свободного места на диске
- Docker установлен и запущен
- SSH доступ с правами суперпользователя или sudo

### Установка Docker на сервере

Выполните следующие команды на сервере:

```bash
# Обновление пакетов
sudo apt update
sudo apt upgrade -y

# Установка зависимостей
sudo apt install -y apt-transport-https ca-certificates curl gnupg lsb-release

# Добавление GPG ключа Docker
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# Добавление репозитория Docker
echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# Установка Docker
sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# Проверка установки
sudo docker --version

# Добавление текущего пользователя в группу docker (чтобы не использовать sudo)
sudo usermod -aG docker $USER
# Выйдите и войдите снова, чтобы изменения вступили в силу
```

### Создание директории для проекта

```bash
# Создание директории
sudo mkdir -p /opt/task_manager/{data,logs,reports}

# Установка прав доступа (замените username на ваше имя пользователя)
sudo chown -R $USER:$USER /opt/task_manager
sudo chmod -R 755 /opt/task_manager
```

---

## Настройка секретов в GitHub

1. Перейдите в ваш GitHub репозиторий
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret** и добавьте следующие секреты:

### SSH_PRIVATE_KEY

Приватный SSH ключ для доступа к серверу.

**Как создать SSH ключ (если у вас его нет):**

```bash
# На локальной машине
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy

# Скопируйте ПРИВАТНЫЙ ключ (файл без .pub)
cat ~/.ssh/github_actions_deploy
```

Скопируйте содержимое файла `github_actions_deploy` (без `.pub`) и вставьте в секрет `SSH_PRIVATE_KEY`.

**Важно:** Сохраните публичный ключ на сервере:

```bash
# На сервере
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ в authorized_keys
echo "ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Или используйте существующий SSH ключ, если он у вас уже есть.

### SSH_HOST

IP-адрес или доменное имя вашего сервера.

**Примеры:**
- `192.168.1.100`
- `example.com`
- `server.example.com`

### SSH_USER

Имя пользователя для SSH подключения.

**Примеры:**
- `root`
- `deploy`
- `ubuntu`
- `admin`

---

## Настройка .env файла на сервере

После первого деплоя на сервере автоматически создастся шаблонный файл `.env`. Необходимо отредактировать его с реальными значениями.

### Подключение к серверу

```bash
ssh your_user@your_server_ip
```

### Редактирование .env файла

```bash
# Откройте файл для редактирования
nano /opt/task_manager/.env
```

Или используйте другой редактор (vim, vi, etc.)

### Заполнение значений

Обновите следующие значения:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=ваш_реальный_токен_бота

# Database Configuration
DATABASE_URL=sqlite:///task_manager.db

# Encryption Key (сгенерируйте новый ключ)
ENCRYPTION_KEY=ваш_32_символьный_ключ_шифрования

# Flask API Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=bot.log
```

### Генерация ключа шифрования

Выполните на сервере или локально:

```bash
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

Скопируйте полученный ключ и вставьте в `ENCRYPTION_KEY`.

### Получение токена Telegram-бота

1. Найдите бота [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot` (или `/mybots` если бот уже создан)
3. Следуйте инструкциям
4. Скопируйте токен и вставьте в `TELEGRAM_BOT_TOKEN`

### Сохранение файла

После редактирования сохраните файл:
- В nano: `Ctrl+O`, затем `Enter`, затем `Ctrl+X`
- В vim: `:wq`, затем `Enter`

### Установка прав доступа

```bash
chmod 600 /opt/task_manager/.env
```

Это защитит файл от чтения посторонними пользователями.

---

## Запуск деплоя

### Автоматический деплой

Деплой запускается автоматически при push в ветку `main`:

```bash
git add .
git commit -m "Deploy changes"
git push origin main
```

### Ручной запуск деплоя

1. Перейдите в GitHub репозиторий
2. Откройте вкладку **Actions**
3. Выберите workflow **Build and Deploy**
4. Нажмите **Run workflow**
5. Выберите ветку (обычно `main`)
6. Нажмите **Run workflow**

### Мониторинг процесса деплоя

1. В разделе **Actions** вы увидите запущенный workflow
2. Дождитесь завершения джобы **Build and Push Docker Image** (зеленая галочка)
3. Дождитесь завершения джобы **Deploy to Server** (зеленая галочка)

---

## Проверка работы

### Проверка контейнеров на сервере

Подключитесь к серверу и выполните:

```bash
# Проверка статуса контейнеров
docker ps

# Вы должны увидеть два контейнера:
# - tg_bot_task_manager
# - tg_bot_task_manager_api
```

### Проверка логов бота

```bash
# Логи бота
docker logs tg_bot_task_manager

# Логи в реальном времени
docker logs -f tg_bot_task_manager

# Логи API
docker logs tg_bot_task_manager_api
```

### Проверка работы API

```bash
# Проверка health endpoint
curl http://localhost:5000/health

# Должен вернуться: {"status":"ok","timestamp":"..."}
```

### Проверка работы бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота по имени
3. Отправьте команду `/start`
4. Бот должен ответить и показать главное меню

### Проверка файлов

```bash
# Проверка наличия директорий
ls -la /opt/task_manager/

# Должны быть директории: data, logs, reports
# И файлы: .env, task_manager.db (создастся после первого запуска)
```

---

## Устранение проблем

### Контейнеры не запускаются

```bash
# Проверьте логи
docker logs tg_bot_task_manager
docker logs tg_bot_task_manager_api

# Проверьте статус контейнеров
docker ps -a

# Перезапустите контейнеры
docker restart tg_bot_task_manager
docker restart tg_bot_task_manager_api
```

### Ошибка подключения к серверу по SSH

1. Проверьте правильность `SSH_HOST` и `SSH_USER` в секретах
2. Убедитесь, что SSH ключ добавлен в `authorized_keys` на сервере
3. Проверьте доступность сервера: `ping your_server_ip`
4. Проверьте SSH подключение вручную: `ssh your_user@your_server_ip`

### Ошибка авторизации в GitHub Container Registry

1. Убедитесь, что в секретах GitHub есть `GITHUB_TOKEN` (создается автоматически)
2. Проверьте, что у репозитория есть права на запись в GitHub Packages

### Бот не отвечает в Telegram

1. Проверьте правильность `TELEGRAM_BOT_TOKEN` в `.env`
2. Проверьте логи бота: `docker logs tg_bot_task_manager`
3. Убедитесь, что контейнер запущен: `docker ps | grep tg_bot_task_manager`

### Ошибка "database locked" или проблемы с БД

```bash
# Остановите контейнеры
docker stop tg_bot_task_manager tg_bot_task_manager_api

# Проверьте файл БД
ls -la /opt/task_manager/task_manager.db

# При необходимости удалите и пересоздайте БД
rm /opt/task_manager/task_manager.db

# Перезапустите контейнеры
docker start tg_bot_task_manager
docker start tg_bot_task_manager_api
```

### Проблемы с правами доступа

```bash
# Исправьте права на директорию
sudo chown -R $USER:$USER /opt/task_manager
sudo chmod -R 755 /opt/task_manager
sudo chmod 600 /opt/task_manager/.env
```

### Просмотр логов на сервере

```bash
# Логи бота (файл)
tail -f /opt/task_manager/logs/bot.log

# Логи контейнера бота
docker logs -f tg_bot_task_manager

# Последние 100 строк логов
docker logs --tail 100 tg_bot_task_manager
```

### Полная переустановка

Если нужно полностью переустановить приложение:

```bash
# Остановите и удалите контейнеры
docker stop tg_bot_task_manager tg_bot_task_manager_api
docker rm tg_bot_task_manager tg_bot_task_manager_api

# Удалите образ
docker rmi ghcr.io/bezngor/tg_bot_task_manager_production:main

# Удалите директорию (осторожно! Это удалит все данные)
sudo rm -rf /opt/task_manager

# Создайте директорию заново
sudo mkdir -p /opt/task_manager/{data,logs,reports}
sudo chown -R $USER:$USER /opt/task_manager

# Запустите деплой заново через GitHub Actions
```

---

## Обновление приложения

Для обновления приложения просто выполните push в ветку `main`:

```bash
git add .
git commit -m "Update application"
git push origin main
```

GitHub Actions автоматически:
1. Соберет новый Docker образ
2. Запушит его в GitHub Container Registry
3. Остановит старые контейнеры
4. Запустит новые контейнеры с обновленным образом

---

## Резервное копирование

Рекомендуется регулярно делать резервные копии:

```bash
# Создание резервной копии БД
cp /opt/task_manager/task_manager.db /opt/task_manager/backup/task_manager_$(date +%Y%m%d_%H%M%S).db

# Создание резервной копии .env (осторожно с секретами!)
cp /opt/task_manager/.env /opt/task_manager/backup/.env.backup

# Создание полной резервной копии
tar -czf /opt/task_manager/backup/full_backup_$(date +%Y%m%d_%H%M%S).tar.gz /opt/task_manager/
```

---

## Дополнительные настройки

### Настройка автозапуска контейнеров при перезагрузке сервера

Контейнеры уже настроены с флагом `--restart unless-stopped`, поэтому они автоматически запустятся при перезагрузке сервера.

### Настройка firewall

Если нужно открыть порт 5000 для API:

```bash
# UFW (Ubuntu)
sudo ufw allow 5000/tcp
sudo ufw reload

# или iptables
sudo iptables -A INPUT -p tcp --dport 5000 -j ACCEPT
```

### Мониторинг ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Использование диска
df -h /opt/task_manager

# Использование памяти
free -h
```

---

## Контакты и поддержка

При возникновении проблем:
1. Проверьте логи контейнеров
2. Проверьте логи GitHub Actions
3. Убедитесь, что все секреты настроены правильно
4. Проверьте, что сервер доступен по SSH

---

**Успешного деплоя! 🚀**
