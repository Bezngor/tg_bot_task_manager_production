# Telegram-бот для управления заданиями цеха

Система управления заданиями для начальника цеха и сотрудников на базе Telegram-бота и REST API.

## Описание

Бот позволяет начальнику цеха создавать задания для сотрудников, отслеживать их выполнение и формировать отчеты. Сотрудники получают задания, подтверждают их получение и отчитываются о фактически выполненной работе.

## Функциональность

### Для начальника (Manager):
- 📋 Создание заданий с выбором оборудования, продукции, количества и сотрудника
- 📊 Просмотр созданных заданий
- 📈 Генерация отчетов в формате CSV и PDF
- 🔔 Получение уведомлений о подтверждении и закрытии заданий

### Для сотрудника (Employee):
- 📋 Просмотр назначенных заданий
- ✅ Подтверждение получения заданий
- 📝 Отчет о фактически выполненной продукции
- 🔔 Получение уведомлений о новых заданиях

## Технологии

- **Python 3.8+**
- **python-telegram-bot** - для работы с Telegram API
- **Flask + Flask-RESTX** - для REST API с Swagger документацией
- **SQLAlchemy** - ORM для работы с базой данных
- **SQLite** - база данных
- **cryptography** - шифрование данных (ФЗ-152)
- **reportlab** - генерация PDF отчетов
- **pandas** - работа с данными для CSV отчетов

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd TG_bot_midex_task_manager_production
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Database Configuration
DATABASE_URL=sqlite:///task_manager.db

# Encryption Key (сгенерируйте ключ длиной 32 символа)
ENCRYPTION_KEY=your_32_character_encryption_key_here

# Flask API Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5050
FLASK_DEBUG=False

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=bot.log
```

### Генерация ключа шифрования

Для генерации безопасного ключа шифрования выполните:

```python
from cryptography.fernet import Fernet
key = Fernet.generate_key()
print(key.decode())
```

Скопируйте полученный ключ в `ENCRYPTION_KEY` в файле `.env`.

### 4. Получение токена Telegram-бота

1. Найдите бота [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `TELEGRAM_BOT_TOKEN` в файле `.env`

## Запуск

### Запуск Telegram-бота

```bash
python -m app.bot.bot
```

или через модуль:

```bash
python -m app.bot.bot
```

### Запуск REST API

В отдельном терминале:

```bash
python -m app.api.api
```

API будет доступен по адресу: `http://localhost:5050`
Swagger документация: `http://localhost:5050/swagger/`

### Запуск админ-панели

```bash
python -m app.admin.admin_panel
```

Админ-панель будет доступна по адресу: `http://localhost:5051`

## Инициализация данных

Для создания тестовых данных (участки, оборудование, продукция) выполните:

```bash
python scripts/init_data.py
```

Или из Python:

```python
from app.core.database import init_sample_data
init_sample_data()
```

## Структура проекта

```
TG_bot_midex_task_manager_production/
├── app/                      # Основной код приложения
│   ├── __init__.py
│   ├── core/                 # Ядро приложения (общий код)
│   │   ├── __init__.py
│   │   ├── config.py         # Конфигурация приложения
│   │   ├── models.py         # Модели данных SQLAlchemy
│   │   ├── database.py       # Модуль работы с БД
│   │   └── utils.py          # Утилиты (шифрование, отчеты, логирование)
│   ├── bot/                  # Telegram бот
│   │   ├── __init__.py
│   │   ├── bot.py            # Основной файл Telegram-бота
│   │   └── handlers/         # Обработчики команд (для будущего расширения)
│   │       └── __init__.py
│   ├── api/                  # REST API
│   │   ├── __init__.py
│   │   ├── api.py            # REST API на Flask
│   │   └── namespaces/       # API namespaces (для будущего расширения)
│   │       └── __init__.py
│   └── admin/                # Админ-панель
│       ├── __init__.py
│       └── admin_panel.py    # Веб-интерфейс админ-панели
├── scripts/                  # Скрипты для запуска и утилиты
│   └── init_data.py          # Скрипт инициализации данных
├── docs/                     # Документация
│   ├── ADMIN_PANEL.md
│   ├── DEPLOYMENT.md
│   ├── USER_GUIDE.md
│   └── ...
├── docker/                   # Docker конфигурации (опционально)
│   ├── Dockerfile
│   └── docker-compose.yml
├── logs/                     # Папка с логами (создается автоматически)
├── reports/                  # Папка с отчетами (CSV, PDF, создается автоматически)
├── data/                     # Папка для данных (опционально)
├── requirements.txt          # Зависимости проекта
├── .env                      # Переменные окружения (не коммитится)
├── .env.example              # Пример файла окружения
├── Dockerfile                # Dockerfile для сборки образа
├── docker-compose.yml        # Docker Compose конфигурация
├── README.md                 # Документация проекта
└── task_manager.db           # База данных SQLite (создается автоматически)
```

## Использование

### Команды бота

После запуска бота, отправьте команду `/start` в Telegram.

#### Для начальника:
- **📋 Создать задание** - создание нового задания
- **📊 Мои задания** - просмотр созданных заданий
- **📈 Отчет** - генерация отчета в CSV/PDF
- **🔔 Уведомления** - просмотр уведомлений

#### Для сотрудника:
- **📋 Мои задания** - просмотр назначенных заданий
- **✅ Подтвердить задание** - подтверждение получения задания
- **📝 Отчитаться** - отчет о выполненной работе
- **🔔 Уведомления** - просмотр уведомлений

### API Endpoints

#### Задания (Tasks)
- `GET /api/tasks` - список заданий
  - Параметры: `manager_id`, `employee_id`, `status`
- `POST /api/tasks` - создание задания
- `GET /api/tasks/<id>` - получение задания по ID
- `PUT /api/tasks/<id>` - обновление задания

#### Пользователи (Users)
- `GET /api/users` - список пользователей
  - Параметры: `role` (admin, manager, employee)
- `GET /api/users/<id>` - получение пользователя по ID

#### Отчеты (Reports)
- `GET /api/reports/generate` - генерация отчета
  - Параметры:
    - `manager_id` (обязательный)
    - `format` (csv или pdf, по умолчанию csv)
    - `date_from` (YYYY-MM-DD)
    - `date_to` (YYYY-MM-DD)

#### Справочники
- `GET /api/equipment` - список оборудования
  - Параметры: `workshop_id`
- `GET /api/products` - список продукции

#### Health Check
- `GET /health` - проверка состояния API

Полная документация API доступна по адресу: `http://localhost:5050/swagger/`

## Роли пользователей

- **admin** - администратор системы (полный доступ)
- **manager** - начальник цеха (создание заданий, просмотр отчетов)
- **employee** - сотрудник (получение заданий, отчеты о работе)

По умолчанию новые пользователи регистрируются как сотрудники. Для изменения роли обратитесь к администратору БД.

## Безопасность (ФЗ-152)

- Персональные данные шифруются с использованием библиотеки `cryptography`
- Ключ шифрования хранится в `.env` файле и не коммитится в репозиторий
- Логирование всех операций для аудита
- Проверка ролей при доступе к функциям

## Смены

- **1-я смена**: с 8:00 до 20:00
- **2-я смена**: с 20:00 до 8:00

## Развертывание на VPS

### 1. Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Python и pip
sudo apt install python3 python3-pip python3-venv -y
```

### 2. Клонирование проекта

```bash
cd /opt
sudo git clone <repository-url> task_manager
cd task_manager
```

### 3. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Создайте `.env` файл с необходимыми значениями.

### 5. Запуск через systemd

Создайте файл `/etc/systemd/system/task-manager-bot.service`:

```ini
[Unit]
Description=Task Manager Telegram Bot
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/task_manager
Environment="PATH=/opt/task_manager/venv/bin"
ExecStart=/opt/task_manager/venv/bin/python -m app.bot.bot
Restart=always

[Install]
WantedBy=multi-user.target
```

Создайте файл `/etc/systemd/system/task-manager-api.service`:

```ini
[Unit]
Description=Task Manager REST API
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/opt/task_manager
Environment="PATH=/opt/task_manager/venv/bin"
ExecStart=/opt/task_manager/venv/bin/python -m app.api.api
Restart=always

[Install]
WantedBy=multi-user.target
```

Запуск сервисов:

```bash
sudo systemctl daemon-reload
sudo systemctl enable task-manager-bot
sudo systemctl enable task-manager-api
sudo systemctl start task-manager-bot
sudo systemctl start task-manager-api
```

Проверка статуса:

```bash
sudo systemctl status task-manager-bot
sudo systemctl status task-manager-api
```

### 6. Настройка Nginx (опционально, для API)

Создайте конфигурацию `/etc/nginx/sites-available/task-manager`:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5050;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Активируйте конфигурацию:

```bash
sudo ln -s /etc/nginx/sites-available/task-manager /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

## Логирование

Логи сохраняются в папке `logs/` и включают:
- Все операции с заданиями
- Ошибки и исключения
- API запросы
- Действия пользователей

## Форматы отчетов

### CSV
Отчеты в формате CSV содержат следующие колонки:
- ID задания
- Дата задания
- Смена
- Начальник
- Сотрудник
- Оборудование
- Продукция
- План, шт
- Факт, шт
- Статус
- Временные метки

### PDF
Отчеты в формате PDF содержат таблицу с основными данными и могут быть легко распечатаны или отправлены по email.

## Поддержка

При возникновении проблем проверьте:
1. Правильность настройки `.env` файла
2. Логи в папке `logs/`
3. Статус сервисов (при развертывании на VPS)
4. Доступность Telegram API

## Лицензия

[Укажите лицензию проекта]

## Автор

[Укажите автора проекта]
