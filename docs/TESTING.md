# Инструкция по тестированию бота после деплоя

После успешного деплоя выполните следующие шаги для проверки работы бота.

## 1. Проверка статуса контейнеров

Подключитесь к серверу по SSH:

```bash
ssh your_user@your_server_ip
```

Проверьте, что контейнеры запущены:

```bash
docker ps
```

Вы должны увидеть два запущенных контейнера:
- `tg_bot_task_manager` (бот)
- `tg_bot_task_manager_api` (API)

Если контейнеры не запущены, проверьте их статус:

```bash
docker ps -a
```

И запустите их вручную, если нужно:

```bash
docker start tg_bot_task_manager
docker start tg_bot_task_manager_api
```

## 2. Проверка логов

### Логи бота

```bash
# Последние 50 строк логов
docker logs --tail 50 tg_bot_task_manager

# Логи в реальном времени
docker logs -f tg_bot_task_manager
```

**Что искать в логах:**
- `Бот запущен и готов к работе` - успешный запуск
- `Ошибка` или `Error` - возможные проблемы
- Подключение к Telegram API

### Логи API

```bash
docker logs --tail 50 tg_bot_task_manager_api
```

**Что искать:**
- `Запуск Flask API на 0.0.0.0:5050` - API запущен
- Ошибки подключения к базе данных

### Файловые логи

```bash
# Логи из файла (если настроены)
tail -f /opt/task_manager/logs/bot.log
```

## 3. Проверка работы API

### Health Check

```bash
curl http://localhost:5050/health
```

**Ожидаемый ответ:**
```json
{"status":"ok","timestamp":"2024-01-15T12:00:00"}
```

### Проверка Swagger документации

Откройте в браузере (если у сервера есть публичный IP или домен):
```
http://your_server_ip:5050/swagger/
```

Или через SSH туннель:
```bash
# На локальной машине
ssh -L 5050:localhost:5050 your_user@your_server_ip
```

Затем откройте в браузере: `http://localhost:5050/swagger/`

### Тест API endpoints

```bash
# Список оборудования
curl http://localhost:5050/api/equipment

# Список продукции
curl http://localhost:5050/api/products

# Список пользователей
curl http://localhost:5050/api/users
```

## 4. Проверка работы бота в Telegram

### Первый запуск

1. Откройте Telegram на вашем устройстве
2. Найдите вашего бота по имени (который вы создали через @BotFather)
3. Нажмите **Start** или отправьте команду `/start`

**Ожидаемое поведение:**
- Бот должен ответить приветственным сообщением
- Появится клавиатура с кнопками меню
- Новый пользователь будет автоматически зарегистрирован как **Сотрудник**

### Проверка регистрации пользователя

После отправки `/start` проверьте, что пользователь создан в базе:

```bash
# На сервере, через API
curl http://localhost:5050/api/users
```

Или проверьте базу данных напрямую:

```bash
# Если установлен sqlite3
sqlite3 /opt/task_manager/task_manager.db "SELECT telegram_id, username, full_name, role FROM users;"
```

## 5. Настройка первого администратора/начальника

По умолчанию новые пользователи регистрируются как **Сотрудники**. Чтобы создать администратора или начальника:

### Вариант 1: Через базу данных

```bash
# Подключитесь к базе данных
sqlite3 /opt/task_manager/task_manager.db

# Найдите ваш Telegram ID (отправьте /start боту и запомните ваш telegram_id)
# Обновите роль пользователя
UPDATE users SET role = 'manager' WHERE telegram_id = YOUR_TELEGRAM_ID;
# или
UPDATE users SET role = 'admin' WHERE telegram_id = YOUR_TELEGRAM_ID;

# Проверьте изменения
SELECT telegram_id, username, full_name, role FROM users;

# Выход
.quit
```

### Вариант 2: Через API (если настроен)

```bash
# Это потребует дополнительной настройки API для изменения ролей
# Пока можно использовать только через БД
```

## 6. Тестирование функционала

### Для сотрудника (Employee)

1. **Просмотр заданий:**
   - Нажмите кнопку **📋 Мои задания**
   - Должен показать список заданий (если они есть)

2. **Подтверждение задания:**
   - Нажмите **✅ Подтвердить задание**
   - Выберите задание из списка
   - Задание должно быть подтверждено

3. **Отчет о работе:**
   - Нажмите **📝 Отчитаться**
   - Выберите задание
   - Введите фактическое количество выполненной продукции

4. **Уведомления:**
   - Нажмите **🔔 Уведомления**
   - Должны показаться все уведомления

### Для начальника (Manager)

1. **Создание задания:**
   - Нажмите **📋 Создать задание**
   - Выберите оборудование
   - Выберите продукцию
   - Введите количество
   - Выберите сотрудника
   - Выберите смену
   - Подтвердите создание

2. **Просмотр заданий:**
   - Нажмите **📊 Мои задания**
   - Должен показать все созданные задания

3. **Генерация отчета:**
   - Нажмите **📈 Отчет**
   - Отчет будет сгенерирован в формате CSV и PDF

4. **Уведомления:**
   - При подтверждении/закрытии задания сотрудником начальник получает уведомление

## 7. Создание тестовых данных

Для полноценного тестирования нужно создать тестовые данные (участки, оборудование, продукцию).

### Вариант 1: Через скрипт (если есть)

```bash
# На сервере
cd /opt/task_manager
# Если есть скрипт инициализации данных
python3 -c "from database import init_sample_data; init_sample_data()"
```

### Вариант 2: Через API

```bash
# Создание участка (нужно будет добавить endpoint в API)
# Или через прямое подключение к БД

sqlite3 /opt/task_manager/task_manager.db << EOF
-- Создание участка
INSERT INTO workshops (name, description) VALUES ('Участок №1', 'Основной участок производства');

-- Создание оборудования
INSERT INTO equipment (name, code, workshop_id, is_active) 
VALUES ('Станок А', 'EQ-001', 1, 1);

-- Создание продукции
INSERT INTO products (name, code, default_equipment_id, is_active) 
VALUES ('Изделие А', 'PRD-001', 1, 1);
EOF
```

### Вариант 3: Через временный Python скрипт

Создайте файл на сервере `/tmp/init_data.py`:

```python
from database import init_sample_data, get_db, close_db

if __name__ == '__main__':
    db = get_db()
    try:
        init_sample_data()
        print("Тестовые данные созданы успешно!")
    finally:
        close_db(db)
```

Затем выполните:

```bash
# Скопируйте database.py на сервер (или выполните внутри контейнера)
docker exec -it tg_bot_task_manager python /path/to/init_data.py
```

## 8. Проверка базы данных

```bash
# Просмотр всех таблиц
sqlite3 /opt/task_manager/task_manager.db ".tables"

# Просмотр пользователей
sqlite3 /opt/task_manager/task_manager.db "SELECT * FROM users;"

# Просмотр участков
sqlite3 /opt/task_manager/task_manager.db "SELECT * FROM workshops;"

# Просмотр оборудования
sqlite3 /opt/task_manager/task_manager.db "SELECT * FROM equipment;"

# Просмотр продукции
sqlite3 /opt/task_manager/task_manager.db "SELECT * FROM products;"

# Просмотр заданий
sqlite3 /opt/task_manager/task_manager.db "SELECT * FROM tasks;"
```

## 9. Типичные проблемы и решения

### Бот не отвечает

**Проверьте:**
1. Логи бота: `docker logs tg_bot_task_manager`
2. Правильность токена в `.env`: `cat /opt/task_manager/.env | grep TELEGRAM_BOT_TOKEN`
3. Доступность интернета на сервере
4. Правильность токена (не должен быть `your_telegram_bot_token_here`)

**Решение:**
```bash
# Отредактируйте .env файл
nano /opt/task_manager/.env
# Убедитесь, что TELEGRAM_BOT_TOKEN содержит реальный токен

# Перезапустите контейнер
docker restart tg_bot_task_manager
```

### API не отвечает

**Проверьте:**
1. Логи API: `docker logs tg_bot_task_manager_api`
2. Порт: `netstat -tulpn | grep 5050` или `ss -tulpn | grep 5050`
3. Firewall: убедитесь, что порт 5050 открыт

**Решение:**
```bash
# Проверьте, что контейнер запущен
docker ps | grep tg_bot_task_manager_api

# Перезапустите при необходимости
docker restart tg_bot_task_manager_api

# Проверьте доступность порта
curl http://localhost:5050/health
```

### Ошибки в логах

**Типичные ошибки:**
- `database locked` - БД используется другим процессом
- `no such table` - база не инициализирована
- `Connection refused` - проблемы с сетью

**Решение:**
```bash
# Остановите все контейнеры
docker stop tg_bot_task_manager tg_bot_task_manager_api

# Инициализируйте БД заново (если нужно)
# Перезапустите контейнеры
docker start tg_bot_task_manager
docker start tg_bot_task_manager_api
```

## 10. Полный цикл тестирования

### Сценарий 1: Создание и выполнение задания

1. **Начальник создает задание:**
   - Откройте бота от имени пользователя с ролью `manager`
   - Создайте задание для сотрудника
   - Проверьте, что задание появилось в БД

2. **Сотрудник получает задание:**
   - Откройте бота от имени сотрудника
   - Проверьте уведомления
   - Подтвердите получение задания

3. **Сотрудник отчитывается:**
   - Завершите работу
   - Отправьте отчет с фактическим количеством

4. **Начальник проверяет:**
   - Проверьте уведомления
   - Сгенерируйте отчет
   - Проверьте, что данные корректны

### Сценарий 2: Генерация отчета

1. Создайте несколько заданий
2. Выполните их с разными результатами
3. Сгенерируйте отчет
4. Проверьте файлы в `/opt/task_manager/reports/`

## 11. Мониторинг работы

### Проверка использования ресурсов

```bash
# Использование CPU и памяти
docker stats

# Использование диска
df -h /opt/task_manager

# Размер базы данных
ls -lh /opt/task_manager/task_manager.db
```

### Регулярная проверка логов

```bash
# Последние ошибки в логах бота
docker logs tg_bot_task_manager 2>&1 | grep -i error | tail -20

# Последние ошибки в логах API
docker logs tg_bot_task_manager_api 2>&1 | grep -i error | tail -20
```

## 12. Быстрая проверка работоспособности

Выполните этот скрипт для быстрой проверки:

```bash
#!/bin/bash
echo "=== Проверка контейнеров ==="
docker ps | grep tg_bot_task_manager

echo -e "\n=== Проверка API ==="
curl -s http://localhost:5050/health | jq '.' || curl -s http://localhost:5050/health

echo -e "\n=== Проверка БД ==="
ls -lh /opt/task_manager/task_manager.db

echo -e "\n=== Последние строки логов бота ==="
docker logs --tail 5 tg_bot_task_manager

echo -e "\n=== Проверка завершена ==="
```

Сохраните как `check_bot.sh` и выполните:
```bash
chmod +x check_bot.sh
./check_bot.sh
```

---

## Следующие шаги

После успешного тестирования:

1. ✅ Убедитесь, что все функции работают корректно
2. ✅ Настройте регулярное резервное копирование БД
3. ✅ Настройте мониторинг (опционально)
4. ✅ Добавьте реальных пользователей
5. ✅ Создайте реальные участки, оборудование и продукцию
6. ✅ Обучите пользователей работе с ботом

**Успешного тестирования! 🚀**
