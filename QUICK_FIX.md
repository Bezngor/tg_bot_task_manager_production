# Быстрое исправление после деплоя

## Проблема: директория не найдена и нет .env файла

Если после деплоя вы не видите директорию `/opt/task_manager`, выполните следующие шаги:

## Шаг 1: Проверка и создание директории

Выполните на сервере:

```bash
# Проверьте, существует ли директория
ls -la /opt/task_manager

# Если директории нет, создайте её
sudo mkdir -p /opt/task_manager/{data,logs,reports}
sudo chown -R $USER:$USER /opt/task_manager
sudo chmod -R 755 /opt/task_manager

# Проверьте создание
ls -la /opt/task_manager
```

## Шаг 2: Создание .env файла

Создайте файл `.env` с правильными значениями:

```bash
# Создайте файл
nano /opt/task_manager/.env
```

Вставьте следующий контент (замените значения на реальные):

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=ваш_реальный_токен_ботфазер_здесь

# Database Configuration
DATABASE_URL=sqlite:///task_manager.db

# Encryption Key (сгенерируйте новый)
ENCRYPTION_KEY=ваш_32_символьный_ключ_здесь

# Flask API Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5050
FLASK_DEBUG=False

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=bot.log
```

**Важно:** 
- Замените `ваш_реальный_токен_ботфазер_здесь` на реальный токен от @BotFather
- Сгенерируйте ключ шифрования (см. ниже)

### Генерация ключа шифрования

Выполните на сервере:

```bash
python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
```

Скопируйте полученный ключ и вставьте в `ENCRYPTION_KEY` в файле `.env`

### Получение токена Telegram бота

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте `/mybots` или `/newbot`
4. Выберите вашего бота
5. Скопируйте токен
6. Вставьте в `TELEGRAM_BOT_TOKEN` в файле `.env`

### Сохранение файла

В nano:
- `Ctrl+O` (сохранить)
- `Enter` (подтвердить имя файла)
- `Ctrl+X` (выйти)

Установите правильные права:

```bash
chmod 600 /opt/task_manager/.env
```

## Шаг 3: Перезапуск контейнеров

После создания .env файла перезапустите контейнеры:

```bash
# Остановите контейнеры
docker stop tg_bot_task_manager tg_bot_task_manager_api

# Запустите заново
docker start tg_bot_task_manager
docker start tg_bot_task_manager_api

# Или перезапустите
docker restart tg_bot_task_manager
docker restart tg_bot_task_manager_api
```

## Шаг 4: Проверка логов

### Просмотр логов бота

```bash
# Последние 50 строк
docker logs --tail 50 tg_bot_task_manager

# Логи в реальном времени
docker logs -f tg_bot_task_manager

# Все логи
docker logs tg_bot_task_manager
```

### Что искать в логах:

✅ **Хорошие признаки:**
- `Бот запущен и готов к работе`
- `Пользователь ... выполнил команду /start`
- Нет ошибок `InvalidToken`

❌ **Плохие признаки:**
- `InvalidToken` - неправильный токен
- `database locked` - проблемы с БД
- `No such file or directory` - отсутствует файл

### Просмотр логов API

```bash
docker logs --tail 50 tg_bot_task_manager_api
```

## Шаг 5: Проверка работы

### Проверка API

```bash
curl http://localhost:5050/health
```

Должен вернуть: `{"status":"ok","timestamp":"..."}`

### Проверка бота в Telegram

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Бот должен ответить

## Шаг 6: Если директория все еще не создается автоматически

Если после перезапуска контейнеров директория все еще не существует, это означает, что она не была создана при деплое. 

Проверьте, что в workflow деплоя была выполнена команда создания директории. Если нет, создайте её вручную (см. Шаг 1).

## Альтернативный способ: проверка через контейнер

Вы можете проверить, видит ли контейнер директорию:

```bash
# Войти в контейнер
docker exec -it tg_bot_task_manager bash

# Проверить директории
ls -la /app/data
ls -la /app/logs
ls -la /app/reports

# Выйти
exit
```

## Проверка монтирования volumes

Проверьте, правильно ли смонтированы volumes:

```bash
docker inspect tg_bot_task_manager | grep -A 10 Mounts
```

Вы должны увидеть монтирования:
- `/opt/task_manager/data` → `/app/data`
- `/opt/task_manager/logs` → `/app/logs`
- `/opt/task_manager/reports` → `/app/reports`
- `/opt/task_manager/task_manager.db` → `/app/task_manager.db`

## Полная команда для быстрого исправления

Выполните этот скрипт для автоматического исправления:

```bash
#!/bin/bash

# Создание директории
sudo mkdir -p /opt/task_manager/{data,logs,reports}
sudo chown -R $USER:$USER /opt/task_manager
sudo chmod -R 755 /opt/task_manager

# Генерация ключа шифрования
ENCRYPTION_KEY=$(python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())" 2>/dev/null)

# Создание .env файла (если его нет)
if [ ! -f /opt/task_manager/.env ]; then
    cat > /opt/task_manager/.env << EOF
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ

# Database Configuration
DATABASE_URL=sqlite:///task_manager.db

# Encryption Key
ENCRYPTION_KEY=$ENCRYPTION_KEY

# Flask API Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5050
FLASK_DEBUG=False

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=bot.log
EOF
    chmod 600 /opt/task_manager/.env
    echo "✅ Файл .env создан. НЕ ЗАБУДЬТЕ ЗАМЕНИТЬ TELEGRAM_BOT_TOKEN!"
else
    echo "⚠️  Файл .env уже существует"
fi

echo "✅ Директория создана: /opt/task_manager"
echo "📝 Отредактируйте /opt/task_manager/.env и замените TELEGRAM_BOT_TOKEN"
echo "🔄 Перезапустите контейнеры: docker restart tg_bot_task_manager tg_bot_task_manager_api"
```

Сохраните как `fix_deploy.sh`, сделайте исполняемым и запустите:

```bash
nano fix_deploy.sh
# Вставьте скрипт выше
chmod +x fix_deploy.sh
./fix_deploy.sh
```

---

**После выполнения этих шагов бот должен заработать!**
